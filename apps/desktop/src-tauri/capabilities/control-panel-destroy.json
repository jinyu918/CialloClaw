{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "control-panel-destroy",
  "description": "Allows the control panel window to destroy itself when the frameless close flow exits the session.",
  "windows": ["control-panel"],
  "permissions": ["core:window:allow-destroy"]
}
