# 协议文档

`packages/protocol` 是共享协议的实现真源，这里用于补充人类可读的说明文档。

建议在本目录持续维护以下内容：

- 实体字段含义与归属边界
- `task` 与 `run` 的映射关系说明
- JSON-RPC 方法行为说明
- Notification 载荷说明
- schema 演进记录
- IPC / Named Pipe 与调试态 HTTP 的边界说明
